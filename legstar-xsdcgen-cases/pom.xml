<?xml version="1.0" encoding="UTF-8"?>
<project>
	<modelVersion>4.0.0</modelVersion>
    <parent>
      <groupId>com.legsem.legstar</groupId>
      <artifactId>Legstar</artifactId>
      <version>SNAPSHOT</version>
    </parent>
	<artifactId>legstar-xsdcgen-cases</artifactId>
	<name>Sample xsdcgen cases</name>
    <version>SNAPSHOT</version>
	<description>Sample cases to use for mainframe consumption.</description>
	<url>http://www.legsem.com/legstar/xsdcgen/</url>
    
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                
                    <!-- This will generate web services bundled a war files.  -->
                    <execution>
                        <id>build-ws</id>
                        <phase>package</phase>
                        <configuration>
                            <tasks>
                                <ant antfile="${basedir}/src/main/ant/build-ws.xml">
                                    <property name="wsname" value="cultureinfo" />
                                </ant>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                
                </executions>
            </plugin>
        </plugins>
    </build>

</project>