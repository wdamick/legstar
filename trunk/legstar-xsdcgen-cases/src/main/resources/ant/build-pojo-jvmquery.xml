<!-- =================================================================================
     This script compiles the Jvmquery POJO sample.
  -->
<project basedir="." default="compile">

	<target name="compile">
	     <mkdir dir="bin"/>
		 <javac  target="1.5"
    	         srcdir="src"
		         destdir="bin"
		         fork="yes"
		 />
	</target>

</project>
