


package com.legstar.test.coxb.MSNSearch.bind;

import com.legstar.coxb.ICobolBinding;
import com.legstar.coxb.common.CComplexBinding;
import com.legstar.coxb.ICobolBinaryBinding;
import com.legstar.coxb.CobolBindingFactory;
import com.legstar.coxb.ICobolBindingFactory;
import com.legstar.coxb.ICobolComplexBinding;
import com.legstar.coxb.host.HostException;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import com.legstar.test.coxb.MSNSearch.DateTimeType;
import com.legstar.test.coxb.MSNSearch.ObjectFactory;

/**
 * LegStar Binding for Complex element :
 *   DateTimeType.
 * 
 * This class was generated by LegStar Binding generator.
 */
public class DateTimeTypeBinding 
             extends CComplexBinding {

    /** Value object to which this cobol complex element is bound. */
    private DateTimeType mValueObject;
  
    /** Indicates that the associated Value object just came from the constructor
     * and doesn't need to be recreated. */
    private boolean mUnusedValueObject = false;
    
    /** Maximum host bytes size for this complex object. */
    public static final int BYTE_LENGTH = 24;
    
    /** Child bound to value object property Year(Integer). */
    public ICobolBinaryBinding _year;
    /** Child bound to value object property Month(Integer). */
    public ICobolBinaryBinding _month;
    /** Child bound to value object property Day(Integer). */
    public ICobolBinaryBinding _day;
    /** Child bound to value object property Hour(Integer). */
    public ICobolBinaryBinding _hour;
    /** Child bound to value object property Minute(Integer). */
    public ICobolBinaryBinding _minute;
    /** Child bound to value object property Second(Integer). */
    public ICobolBinaryBinding _second;
            
    /** Logger. */
    private final Log _log = LogFactory.getLog(getClass());

    /** Binding factory. */
    private static final ICobolBindingFactory BF
        = CobolBindingFactory.getBindingFactory();
 
    /** Static reference to Value object factory to be used as default. */
    private static final ObjectFactory JF = new ObjectFactory();
    
    /** Current Value object factory (Defaults to the static one but can be
     *  changed). */
    private ObjectFactory mValueObjectFactory = JF;
    
    /**
     * Constructor for a root Complex element without a bound Value object.
     */
    public DateTimeTypeBinding() {
        this(null);
    }

    /**
     * Constructor for a root Complex element with a bound Value object.
     * 
     * @param valueObject the concrete Value object instance bound to this
     *        complex element
     */
    public DateTimeTypeBinding(
            final DateTimeType valueObject) {
        this("", "", null, valueObject);
    }

    /**
    * Constructor for a Complex element as a child of another element and
    * an associated Value object.
    * 
    * @param bindingName the identifier for this binding
    * @param fieldName field name in parent Value object
    * @param valueObject the concrete Value object instance bound to this
    *        complex element
    * @param parentBinding a reference to the parent binding
    */
    public DateTimeTypeBinding(
            final String bindingName,
            final String fieldName,
            final ICobolComplexBinding parentBinding,
            final DateTimeType valueObject) {
        
        super(bindingName, fieldName, DateTimeType.class, null, parentBinding);
        mValueObject = valueObject;
        if (mValueObject != null) {
            mUnusedValueObject = true;
        }
        initChildren();
        setByteLength(BYTE_LENGTH);
    }

    /** Creates a binding property for each child. */
    private void initChildren() {
        if (_log.isDebugEnabled()) {
            _log.debug("Initializing started");
        }
        /* Create binding children instances */

        _year = BF.createBinaryBinding("Year",
               "Year", Integer.class, this);
        _year.setCobolName("Year");
        _year.setByteLength(4);
        _year.setTotalDigits(9);
        _year.setIsSigned(true);
        _month = BF.createBinaryBinding("Month",
               "Month", Integer.class, this);
        _month.setCobolName("Month");
        _month.setByteLength(4);
        _month.setTotalDigits(9);
        _month.setIsSigned(true);
        _day = BF.createBinaryBinding("Day",
               "Day", Integer.class, this);
        _day.setCobolName("R-Day");
        _day.setByteLength(4);
        _day.setTotalDigits(9);
        _day.setIsSigned(true);
        _hour = BF.createBinaryBinding("Hour",
               "Hour", Integer.class, this);
        _hour.setCobolName("Hour");
        _hour.setByteLength(4);
        _hour.setTotalDigits(9);
        _hour.setIsSigned(true);
        _minute = BF.createBinaryBinding("Minute",
               "Minute", Integer.class, this);
        _minute.setCobolName("Minute");
        _minute.setByteLength(4);
        _minute.setTotalDigits(9);
        _minute.setIsSigned(true);
        _second = BF.createBinaryBinding("Second",
               "Second", Integer.class, this);
        _second.setCobolName("Second");
        _second.setByteLength(4);
        _second.setTotalDigits(9);
        _second.setIsSigned(true);

        /* Add children to children list */
        getChildrenList().add(_year);
        getChildrenList().add(_month);
        getChildrenList().add(_day);
        getChildrenList().add(_hour);
        getChildrenList().add(_minute);
        getChildrenList().add(_second);
 
        if (_log.isDebugEnabled()) {
            _log.debug("Initializing successful");
        }
    }
    
    /** {@inheritDoc} */
    public void createValueObject() throws HostException {
        /* Since this complex binding has a constructor that takes a
         * Value object, we might already have a Value object that
         * was not used yet. */
        if (mUnusedValueObject && mValueObject != null) {
            mUnusedValueObject = false;
            return;
        }
        mValueObject = mValueObjectFactory.createDateTimeType();
    }

    /** {@inheritDoc} */
    public void setChildrenValues() throws HostException {

         /* Make sure there is an associated Value object*/
        if (mValueObject == null) {
            createValueObject();
        }
        /* Get Value object property _year */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_year"
                    + " value=" + mValueObject.getYear());
        }
        _year.setObjectValue(mValueObject.getYear());
        /* Get Value object property _month */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_month"
                    + " value=" + mValueObject.getMonth());
        }
        _month.setObjectValue(mValueObject.getMonth());
        /* Get Value object property _day */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_day"
                    + " value=" + mValueObject.getDay());
        }
        _day.setObjectValue(mValueObject.getDay());
        /* Get Value object property _hour */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_hour"
                    + " value=" + mValueObject.getHour());
        }
        _hour.setObjectValue(mValueObject.getHour());
        /* Get Value object property _minute */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_minute"
                    + " value=" + mValueObject.getMinute());
        }
        _minute.setObjectValue(mValueObject.getMinute());
        /* Get Value object property _second */
        if (_log.isDebugEnabled()) {
            _log.debug("Getting value from Value object property "
                    + "_second"
                    + " value=" + mValueObject.getSecond());
        }
        _second.setObjectValue(mValueObject.getSecond());
     }

    /** {@inheritDoc} */
    public void setPropertyValue(final int index) throws HostException {
 
        ICobolBinding child = getChildrenList().get(index);
        
       /* Children that are not bound to a value object are ignored.
        * This includes Choices and dynamically generated counters
        * for instance.  */
        if (!child.isBound()) {
            return;
        }
        
        /* Set the Value object property value from binding object */
        Object bindingValue = null;
        switch (index) {
        case 0:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setYear((Integer) bindingValue);
            break;
        case 1:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setMonth((Integer) bindingValue);
            break;
        case 2:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setDay((Integer) bindingValue);
            break;
        case 3:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setHour((Integer) bindingValue);
            break;
        case 4:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setMinute((Integer) bindingValue);
            break;
        case 5:
            bindingValue = child.getObjectValue(Integer.class);
            mValueObject.setSecond((Integer) bindingValue);
            break;
         default:
            break;
        }
        if (_log.isDebugEnabled()) {
            _log.debug("Setting value of Value object property "
                    + child.getJaxbName()
                    + " value=" + bindingValue);
        }
    }

    /** {@inheritDoc} */
    public Object getObjectValue(
            final Class < ? > type) throws HostException {
        if (type.equals(DateTimeType.class)) {
            return mValueObject;
        } else {
            throw new HostException("Attempt to get binding " + getBindingName()
                    + " as an incompatible type " + type);
        }
    }

    /** {@inheritDoc} */
    public void setObjectValue(
            final Object bindingValue) throws HostException {
        if (bindingValue == null) {
            mValueObject = null;
            return;
        }
        if (bindingValue.getClass().equals(DateTimeType.class)) {
            mValueObject = (DateTimeType) bindingValue;
        } else {
            throw new HostException("Attempt to set binding " + getBindingName()
                    + " from an incompatible value " + bindingValue);
        }
    }

    /**
     * @return the java object factory for objects creation
     */
    public ObjectFactory getObjectFactory() {
        return mValueObjectFactory;
    }

    /**
     * @param valueObjectFactory the java object factory for objects creation 
     */
    public void setObjectFactory(final Object valueObjectFactory) {
        mValueObjectFactory = (ObjectFactory) valueObjectFactory;
    }

    /** {@inheritDoc} */
    public boolean isSet() {
        return (mValueObject != null);
    }

    /**
     * @return the bound Value object
     */
    public DateTimeType getDateTimeType() {
        return mValueObject;
    }
    
    /**
     * The COBOL complex element maximum length in bytes.
     * 
     * @return COBOL complex element maximum length in bytes
     */
    public int getByteLength() {
        return BYTE_LENGTH;
    }
}

