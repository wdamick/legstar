

package com.legstar.test.coxb.lsfileal.bind;

import com.legstar.coxb.ICobolBinding;
import com.legstar.coxb.common.CChoiceBinding;
import com.legstar.coxb.ICobolComplexBinding;
import com.legstar.test.coxb.lsfileal.ReplySuccessHeader;
import com.legstar.test.coxb.lsfileal.ReplyErrorHeader;
import com.legstar.coxb.host.HostException;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

import com.legstar.test.coxb.lsfileal.ReplyData;

/**
 * LegStar Binding for Choice element:
 *   ReplySuccessHeader
 *   ReplyErrorHeader
 * 
 * Represents a choice between 2 or more elements. A choice maps to a cobol
 * REDEFINES clause exposed as an xs:choice in the corresponding XML schema
 * 
 * This class was generated by LegStar Binding generator.
 */
public class ReplySuccessHeaderChoiceBinding 
             extends CChoiceBinding {

    /** Alternative bound bound to value object property ReplySuccessHeader(ReplySuccessHeader). */
    public ICobolComplexBinding _replySuccessHeader;
    /** Alternative bound bound to value object property ReplyErrorHeader(ReplyErrorHeader). */
    public ICobolComplexBinding _replyErrorHeader;

    /** Logger. */
    private final Log _log = LogFactory.getLog(getClass());

    
    /**
     * Constructor for a Choice element.
     * 
     * @param bindingName the identifier for this binding
     * @param parentBinding a reference to the parent binding
     */
    public ReplySuccessHeaderChoiceBinding(
            final String bindingName,
            final ICobolComplexBinding parentBinding) {
        
        super(bindingName, null, parentBinding);
        initAlternatives();
    }

    /** Creates a binding property for each alternative. */
    private void initAlternatives() {
        if (_log.isDebugEnabled()) {
            _log.debug("Initializing started");
        }
        /* Create binding alternatives instances */

        _replySuccessHeader = new ReplySuccessHeaderBinding("ReplySuccessHeader",
               "ReplySuccessHeader", getParentBinding(), null);
        _replySuccessHeader.setCobolName("REPLY-SUCCESS-HEADER");
        _replySuccessHeader.setByteLength(136);
        _replyErrorHeader = new ReplyErrorHeaderBinding("ReplyErrorHeader",
               "ReplyErrorHeader", getParentBinding(), null);
        _replyErrorHeader.setCobolName("REPLY-ERROR-HEADER");
        _replyErrorHeader.setByteLength(136);
        _replyErrorHeader.setRedefines("REPLY-SUCCESS-HEADER");

        /* Add alternatives to alternatives list */
        addAlternative(_replySuccessHeader);
        addAlternative(_replyErrorHeader);
 
        if (_log.isDebugEnabled()) {
            _log.debug("Initializing successful");
        }
    }
 
    /** {@inheritDoc} */
    public void setAlternativesValues() throws HostException {
        Object value;
        value = getReplyData().getReplySuccessHeader();
        if (value != null) {
            if (_log.isDebugEnabled()) {
                _log.debug("Getting value from Value object property "
                        + "ReplySuccessHeader"
                        + " value=" + value);
            }
            _replySuccessHeader.setObjectValue(value);
        }
        value = getReplyData().getReplyErrorHeader();
        if (value != null) {
            if (_log.isDebugEnabled()) {
                _log.debug("Getting value from Value object property "
                        + "ReplyErrorHeader"
                        + " value=" + value);
            }
            _replyErrorHeader.setObjectValue(value);
        }

    }
    /** {@inheritDoc} */
    public void setPropertyValue(
            final int index) throws HostException {
        /* Set the Value object property value from binding object */
        ICobolBinding alt = getAlternativesList().get(index);

      /* Alternatives that are not bound to a value object property are ignored.
       * This includes Choices and dynamically generated counters
       * for instance.  */
        if (!alt.isBound()) {
            return;
        }

        Object bindingValue = alt.getObjectValue(alt.getJaxbType());
        if (_log.isDebugEnabled()) {
            _log.debug("Setting value of Value object property "
                    + alt.getJaxbName()
                    + " value=" + bindingValue);
        }
        switch (index) {
        case 0:
            getReplyData().setReplySuccessHeader(
                (ReplySuccessHeader) bindingValue);
            break;
        case 1:
            getReplyData().setReplyErrorHeader(
                (ReplyErrorHeader) bindingValue);
            break;
        default:
            break;
        }
    }
    
    /** {@inheritDoc} */
    public Object getObjectValue(
            final Class < ? >  type) throws HostException {
        throw new HostException("Attempt to get value from choice binding "
                + getCobolName());
    }

    /** {@inheritDoc} */
    public void setObjectValue(final Object value) throws HostException {
        throw new HostException("Attempt to set value for choice binding "
                + getCobolName());
    }
    
    /** {@inheritDoc} */
    public boolean isSet() {
        /* A Choice is considered set if at least one of its alternatives
         * is set. */
        for (ICobolBinding alt : getAlternativesList()) {
            if (alt.isSet()) {
                return true;
            }
        }
        return false;
    }

    /** {@inheritDoc} */
    public Object getParentValueObject() throws HostException {
        return ((ReplyDataBinding) getParentBinding()).getReplyData();
    }

    /**
     * @return the Value object bound to the parent of this choice
     * @throws HostException if parent bound Value object cannot be retrieved
     */
    public ReplyData getReplyData() throws HostException {
        return (ReplyData) getParentValueObject();
    }
    
}

