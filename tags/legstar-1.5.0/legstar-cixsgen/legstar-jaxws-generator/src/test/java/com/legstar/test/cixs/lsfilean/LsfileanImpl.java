package com.legstar.test.cixs.lsfilean;
import java.rmi.server.UID;
import javax.jws.WebService;

import com.legstar.coxb.transform.HostTransformException;
import com.legstar.host.invoke.AbstractServiceAdapter;
import com.legstar.host.invoke.HostInvokerException;
import com.legstar.messaging.LegStarAddress;
import com.legstar.test.coxb.lsfileae.Dfhcommarea;
import com.legstar.test.cixs.lsfilean.oper.LsfileaeProgramInvoker;
import com.legstar.test.cixs.lsfilean.oper.LsfileaeFault;
import com.legstar.test.cixs.lsfilean.oper.LsfileaeFaultInfo;

/**
 * JAX-WS Service Adapter implementation.
 * Each method maps to a CICS program. 
 * 
 * This class was generated by LegStar Mainframe Web Service adapter generator.
 */
@WebService(endpointInterface = "com.legstar.test.cixs.lsfilean.Lsfilean",
            serviceName = "lsfileanService",
            portName = "lsfileanPort",
            targetNamespace = "http://cixs.test.legstar.com/lsfilean")
public class LsfileanImpl extends AbstractServiceAdapter implements Lsfilean {

    /** Name of this service adapter implementation. */
    private static final String  SERVICE_ADAPTER_NAME = "lsfilean";

    /** Invoker implementation for operation lsfileae. */
    private LsfileaeProgramInvoker mLsfileaeProgramInvoker;

    /** Contructor creates a set of operation invokers. */
    public LsfileanImpl() {
        super(SERVICE_ADAPTER_NAME);
        mLsfileaeProgramInvoker = new LsfileaeProgramInvoker(getConfigFileName());
    }
    
    /** {@inheritDoc} */
    public Dfhcommarea lsfileae(
               final Dfhcommarea request,
               final LsfileanHostHeader hostHeader)
               throws LsfileaeFault {
    
        try {
            return getLsfileaeProgramInvoker().lsfileae(
                    getAddress(hostHeader), getRequestID(hostHeader), request);
        } catch (HostInvokerException e) {
            throw getLsfileaeFault(e, "Failed to invoke host program:");
        } catch (HostTransformException e) {
            throw getLsfileaeFault(e, "Failed to transform data:");
        }
    }

    /**
     * Formats a fault element to notify client of an exception.
     *
     * @param e the exception which occurred
     * @param text short message describing the context
     * @return the fault exception
     */
    public LsfileaeFault getLsfileaeFault(
            final Exception e, final String text) {

        LsfileaeFaultInfo faultInfo = new LsfileaeFaultInfo();
        faultInfo.setMessage(e.getMessage());
        faultInfo.setDetail(getLsfileaeProgramInvoker().toString());
        return new LsfileaeFault(text + ' ' 
                + faultInfo.getMessage(), faultInfo, e);
    }

        
    /**
     * Extracts header data from SOAP header and create a host address.
     * @param hostHeader the java object mapping the SOAP header element
     * @return the new host address
     */
    public LegStarAddress getAddress(
            final LsfileanHostHeader hostHeader) {
        if (hostHeader == null) {
            return null;
        }
        LegStarAddress address = new LegStarAddress(hostHeader.getHostEndPoint());
        address.setHostCharset(hostHeader.getHostCharset());
        address.setHostUserID(hostHeader.getHostUserID());
        address.setHostPassword(hostHeader.getHostPassword());
        address.setHostTraceMode(hostHeader.getHostTraceMode());
        return address;
    }

    /**
     * Generates a unique ID for a request. This can be passed from the client
     * using the host header.
     * @param hostHeader the java object mapping the SOAP header element
     * @return  a unique request ID
     */
    public String getRequestID(final LsfileanHostHeader hostHeader) {
        if (hostHeader != null && hostHeader.getHostRequestID() != null) {
            return hostHeader.getHostRequestID();
        } else {
            return getServiceAdapterName() + ":" + new UID().toString();
        }
    }

    /**
     * @return the invoker implementation for operation lsfileae
     */
    public LsfileaeProgramInvoker getLsfileaeProgramInvoker() {
        return mLsfileaeProgramInvoker;
    }

    /**
     * @param programInvoker the invoker implementation for operation lsfileae to set
     */
    public void setLsfileaeProgramInvoker(
            final LsfileaeProgramInvoker programInvoker) {
        mLsfileaeProgramInvoker = programInvoker;
    }
    
}
