package com.legstar.test.cixs.varar021;
import com.legstar.coxb.transform.HostTransformException;
import com.legstar.host.invoke.AbstractProgramInvoker;
import com.legstar.host.invoke.HostInvoker;
import com.legstar.host.invoke.HostInvokerException;
import com.legstar.messaging.LegStarAddress;
import com.legstar.test.coxb.varar021.SearchGrplst;
import com.legstar.test.coxb.varar021.bind.SearchGrplstTransformers;

/**
 * Implements an invoker for program VARAR021.
 * <p/>
 * This host program maps to operation varar021.
 * 
 * This class was generated by LegStar Mainframe Web Service adapter generator.
 *
 */
public class Varar021ProgramInvoker extends AbstractProgramInvoker {

    /** Host transformers for SearchGrplst java data object. */
    private SearchGrplstTransformers mSearchGrplstTransformers;

    /** Operation name for varar021. */
    private static final String  OPERATION_NAME = "varar021";

    /**
     * Operation invoker constructor.
     * @param configFileName invoker configuration file name
     */
    public Varar021ProgramInvoker(final String configFileName) {
        super(configFileName, OPERATION_NAME, new Varar021HostProgram());
        mSearchGrplstTransformers = new SearchGrplstTransformers();
    }

    /**
     * Invoke the VARAR021 host program.
     * <p/>
     * The sequence of operations performed is:
     * <ul>
     * <li>Get an invoker for the target host address and program</li>
     * <li>Transform request data object into host bytes</li>
     * <li>Invoke the remote program</li>
     * <li>Transform reply bytes into java data object</li>
     * </ul>
     * @param address target host address
     * @param requestID a unique identifier for this request
     * @param request the request java data object 
     * @return the reply java data object
     * @throws HostInvokerException if host invoker cannot be created or configured
     * @throws HostTransformException if transformation fails
     */
    public SearchGrplst varar021(
            final LegStarAddress address,
            final String requestID,
            final SearchGrplst request)
    throws HostInvokerException, HostTransformException {

        HostInvoker hostInvoker = getHostInvoker(address);
        String hostCharset = hostInvoker.getAddress().getHostCharset();

        /* Translate java object to a host byte array */
        byte[] requestBytes =
            getSearchGrplstTransformers().toHost(request, hostCharset);

        /* Invoke remote program */
        byte[] replyBytes = hostInvoker.invoke(requestID, requestBytes);

        /* Translate host byte array to a java object */
        return getSearchGrplstTransformers().toJava(replyBytes, hostCharset);

    }

    /**
     * @return the host transformers for SearchGrplst
     */
    public SearchGrplstTransformers getSearchGrplstTransformers() {
        return mSearchGrplstTransformers;
    }

    /**
     * @param transformers the host transformers for SearchGrplst
     */
    public void setSearchGrplstTransformers(
            final SearchGrplstTransformers transformers) {
        mSearchGrplstTransformers = transformers;
    }

}
