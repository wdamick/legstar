package com.legstar.test.cixs.lsfileal;
import java.rmi.server.UID;
import javax.jws.WebService;

import com.legstar.coxb.transform.HostTransformException;
import com.legstar.host.invoke.AbstractServiceAdapter;
import com.legstar.host.invoke.HostInvokerException;
import com.legstar.messaging.LegStarAddress;
import com.legstar.test.coxb.lsfileal.RequestParms;
import com.legstar.test.coxb.lsfileal.ReplyData;

/**
 * JAX-WS Service Adapter implementation.
 * Each method maps to a CICS program. 
 * 
 * This class was generated by LegStar Mainframe Web Service adapter generator.
 */
@WebService(endpointInterface = "com.legstar.test.cixs.lsfileal.Lsfileal",
            serviceName = "lsfilealService",
            portName = "lsfilealPort",
            targetNamespace = "http://cixs.test.legstar.com/lsfileal")
public class LsfilealImpl extends AbstractServiceAdapter implements Lsfileal {

    /** Name of this service adapter implementation. */
    private static final String  SERVICE_ADAPTER_NAME = "lsfileal";

    /** Invoker implementation for operation lsfileal. */
    private LsfilealProgramInvoker mLsfilealProgramInvoker;

    /** Contructor creates a set of operation invokers. */
    public LsfilealImpl() {
        super(SERVICE_ADAPTER_NAME);
        mLsfilealProgramInvoker = new LsfilealProgramInvoker(getConfigFileName());
    }
    
    /** {@inheritDoc} */
    public ReplyData lsfileal(
               final RequestParms request,
               final LsfilealHostHeader hostHeader)
               throws LsfilealFault {
    
        try {
            return getLsfilealProgramInvoker().lsfileal(
                    getAddress(hostHeader), getRequestID(hostHeader), request);
        } catch (HostInvokerException e) {
            throw getLsfilealFault(e, "Failed to invoke host program:");
        } catch (HostTransformException e) {
            throw getLsfilealFault(e, "Failed to transform data:");
        }
    }

    /**
     * Formats a fault element to notify client of an exception.
     *
     * @param e the exception which occurred
     * @param text short message describing the context
     * @return the fault exception
     */
    public LsfilealFault getLsfilealFault(
            final Exception e, final String text) {

        LsfilealFaultInfo faultInfo = new LsfilealFaultInfo();
        faultInfo.setMessage(e.getMessage());
        faultInfo.setDetail(getLsfilealProgramInvoker().toString());
        return new LsfilealFault(text + ' ' 
                + faultInfo.getMessage(), faultInfo, e);
    }

        
    /**
     * Extracts header data from SOAP header and create a host address.
     * @param hostHeader the java object mapping the SOAP header element
     * @return the new host address
     */
    public LegStarAddress getAddress(
            final LsfilealHostHeader hostHeader) {
        if (hostHeader == null) {
            return null;
        }
        LegStarAddress address = new LegStarAddress(hostHeader.getHostEndPoint());
        address.setHostCharset(hostHeader.getHostCharset());
        address.setHostUserID(hostHeader.getHostUserID());
        address.setHostPassword(hostHeader.getHostPassword());
        address.setHostTraceMode(hostHeader.getHostTraceMode());
        return address;
    }

    /**
     * Generates a unique ID for a request. This can be passed from the client
     * using the host header.
     * @param hostHeader the java object mapping the SOAP header element
     * @return  a unique request ID
     */
    public String getRequestID(final LsfilealHostHeader hostHeader) {
        if (hostHeader != null && hostHeader.getHostRequestID() != null) {
            return hostHeader.getHostRequestID();
        } else {
            return getServiceAdapterName() + ":" + new UID().toString();
        }
    }

    /**
     * @return the invoker implementation for operation lsfileal
     */
    public LsfilealProgramInvoker getLsfilealProgramInvoker() {
        return mLsfilealProgramInvoker;
    }

    /**
     * @param programInvoker the invoker implementation for operation lsfileal to set
     */
    public void setLsfilealProgramInvoker(
            final LsfilealProgramInvoker programInvoker) {
        mLsfilealProgramInvoker = programInvoker;
    }
    
}
